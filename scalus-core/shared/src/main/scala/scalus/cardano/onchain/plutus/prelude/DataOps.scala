package scalus.cardano.onchain.plutus.prelude

//import scalus.uplc.builtin.{FromData, ToData}

type BuiltinData = scalus.uplc.builtin.Data
//val Data = scalus.uplc.builtin.Data

//extension [A: ToData](a: A) inline def toData: Data = summon[ToData[A]](a)
//extension (inline data: Data) inline def fromData[A](using inline ev: FromData[A]): A = ev(data)
